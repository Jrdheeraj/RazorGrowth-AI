# ============================================================================
# RazorGrowth AI — Python dependencies
#
# Direct runtime and development dependencies, verified against the actual
# repository imports (no unused packages). Frontend (npm) dependencies live
# in frontend/package.json.
# ============================================================================

# ── Core framework ──────────────────────────────────────────────────────────
fastapi==0.141.1                 # backend API framework (routes, deps, validation)
uvicorn[standard]==0.52.4         # ASGI server: uvicorn backend.app.main:app

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.52               # ORM, engine, tenant-scoped models
psycopg2-binary==2.9.12          # PostgreSQL DBAPI driver for SQLAlchemy
alembic==1.16.4                   # schema migrations (migrations/versions)
pgvector==0.3.6                   # VECTOR column type for the knowledge store

# ── Validation / settings ───────────────────────────────────────────────────
pydantic==2.13.4                  # request/response schemas, model validation
pydantic-settings==2.15.0         # .env-backed application settings (core/config.py)

# ── Authentication ──────────────────────────────────────────────────────────
PyJWT==2.7.0                      # JWT access-token signing/verification (core/security.py)

# ── HTTP / networking ───────────────────────────────────────────────────────
httpx==0.28.1                     # HTTP client (tooling, tests, integration paths)

# ── Payments ────────────────────────────────────────────────────────────────
razorpay==2.0.1                   # Razorpay TEST-mode orders, payments, webhooks

# ── AI / LLM ────────────────────────────────────────────────────────────────
openai==1.58.1                    # OpenAI SDK powering the provider layer
                                  # (OpenRouter free-only / Groq / OpenAI — backend/app/ai/llm)

# ── Testing ─────────────────────────────────────────────────────────────────
pytest==9.1.1                     # backend test suite (673 tests, SQLite-compatible)
